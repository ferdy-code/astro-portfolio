---
import Badge from "./Badge.astro";
import { cn } from "../../lib/utils";

interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<Badge class={cn("uppercase", className)} {...rest}>
  <slot />
</Badge>
